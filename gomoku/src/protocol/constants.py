from sys import stdin, stdout, stderr

# IO
INPUT_STREAM = stdin
OUTPUT_STREAM = stdout
ERROR_STREAM = stdout

# Commands
COMMAND_SPLITTER = r' |\n+'
PARAMS_SPLITTER = r' |,+'
RETURN_LINE = '\n'
DONE = 'DONE'
